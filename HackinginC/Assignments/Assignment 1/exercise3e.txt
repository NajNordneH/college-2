I wrote a short bash script which does a 10-part for loop, which takes the output of running the C program,
and pipelining it through the wc command, and counting for new-lines.
It then takes this output, and appends it to the text file
